import { motion } from 'framer-motion'
import { ArrowRight, CircleDollarSign, KeyRound } from 'lucide-react'
import { trackEvent } from '../utils/tracking'
export function PaymentPlan({onEnquire}:{onEnquire:()=>void}){const open=()=>{trackEvent('payment_plan_click');onEnquire()};return <section id="payment-plan" className="section bg-[#11110f] text-white"><div className="container grid gap-10 lg:grid-cols-[.85fr_1.15fr]"><div><p className="eyebrow">Flexible ownership</p><h2 className="display text-5xl leading-tight">Own a Bayview Residence with a <span className="text-[#c8a15a]">10:90</span> Payment Plan.</h2><p className="mt-6 max-w-lg text-sm leading-7 text-white/65">A thoughtfully structured plan that lets you secure your preferred residence today with confidence.</p><button onClick={open} className="btn-gold mt-8 !bg-[#c8a15a] !text-[#11110f]">Get payment plan details <ArrowRight size={16}/></button><p className="mt-4 max-w-lg text-[10px] leading-5 text-white/45">*Payment plan availability, eligibility and terms are subject to developer approval and may change without notice.</p></div><div className="relative grid gap-5 md:grid-cols-2"><div className="absolute left-1/4 right-1/4 top-[30%] hidden h-px bg-[#c8a15a]/60 md:block"/>{[['01','Pay 10% Now','Secure your preferred residence with an initial 10% payment.',CircleDollarSign],['02','Pay 90% on Possession*','Complete the balance on possession, subject to applicable terms.',KeyRound]].map(([number,title,copy,Icon],index)=>{const I=Icon as typeof KeyRound;return <motion.article initial={{opacity:0,y:24}} whileInView={{opacity:1,y:0}} viewport={{once:true}} transition={{delay:index*.15}} key={title as string} className="relative border border-white/15 bg-white/[.035] p-8"><span className="grid h-12 w-12 place-items-center rounded-full border border-[#c8a15a]/60 bg-[#11110f] text-[#c8a15a]"><I size={20}/></span><p className="mt-8 text-xs font-bold tracking-[.2em] text-[#c8a15a]">{number as string}</p><h3 className="display mt-3 text-3xl">{title as string}</h3><p className="mt-4 text-sm leading-6 text-white/60">{copy as string}</p></motion.article>})}</div></div></section>}
